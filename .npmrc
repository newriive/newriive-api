# Use a specific registry
registry=https://registry.npmjs.org/

# Save exact versions
save-exact=true

# Set a custom cache directory
cache=~/.npm

# Always authenticate when using a private registry
always-auth=true
